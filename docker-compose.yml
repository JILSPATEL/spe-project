services:

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    container_name: ecommerce_backend
    restart: unless-stopped

    extra_hosts:
      - "host.docker.internal:host-gateway"

    environment:
      PORT: 5000
      DB_HOST: host.docker.internal
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: Jils@1863
      DB_NAME: ecommerce_solution
      FRONTEND_URL: http://localhost:3000
      JWT_SECRET: change_this_in_production
      NODE_ENV: production

    # ❌ backend not exposed (internal only)
    # ports removed

    networks:
      - frontend-net


  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: /api

    container_name: ecommerce_frontend
    restart: unless-stopped

    ports:
      - "3000:80"

    depends_on:
      - backend

    networks:
      - frontend-net


networks:
  frontend-net: