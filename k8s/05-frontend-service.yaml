apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: ecommerce
    component: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 5173
      targetPort: 5173
      nodePort: 30001
      protocol: TCP
