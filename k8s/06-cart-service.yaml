apiVersion: v1
kind: Service
metadata:
  name: cart-service
  labels:
    app: ecommerce
    component: cart-service
spec:
  selector:
    app: cart-service
  ports:
    - port: 5004
      targetPort: 5004
      protocol: TCP
