apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: ecommerce
    component: order-service
spec:
  selector:
    app: order-service
  ports:
    - port: 5005
      targetPort: 5005
      protocol: TCP
