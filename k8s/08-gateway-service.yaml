apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    app: ecommerce
    component: api-gateway
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30000
      protocol: TCP
