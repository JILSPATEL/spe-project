apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: ecommerce
    component: user-service
spec:
  type: NodePort
  selector:
    app: user-service
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30011
      protocol: TCP
