apiVersion: v1
kind: Service
metadata:
  name: seller-service
  labels:
    app: ecommerce
    component: seller-service
spec:
  selector:
    app: seller-service
  ports:
    - port: 5002
      targetPort: 5002
      protocol: TCP
