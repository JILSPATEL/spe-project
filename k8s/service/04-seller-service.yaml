apiVersion: v1
kind: Service
metadata:
  name: seller-service
  labels:
    app: ecommerce
    component: seller-service
spec:
  type: NodePort
  selector:
    app: seller-service
  ports:
    - port: 5002
      targetPort: 5002
      nodePort: 30012
      protocol: TCP
