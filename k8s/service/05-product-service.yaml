apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: ecommerce
    component: product-service
spec:
  type: NodePort
  selector:
    app: product-service
  ports:
    - port: 5003
      targetPort: 5003
      nodePort: 30013
      protocol: TCP
