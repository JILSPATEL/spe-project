apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: ecommerce
    component: product-service
spec:
  selector:
    app: product-service
  ports:
    - port: 5003
      targetPort: 5003
      protocol: TCP
