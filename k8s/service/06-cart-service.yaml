apiVersion: v1
kind: Service
metadata:
  name: cart-service
  labels:
    app: ecommerce
    component: cart-service
spec:
  type: NodePort
  selector:
    app: cart-service
  ports:
    - port: 5004
      targetPort: 5004
      nodePort: 30014
      protocol: TCP
